---
description: Initialize a new project from this template. Runs a guided conversation to generate all project-specific documentation (business domain, architecture, tech stack, best practices) and opens a setup PR.
---

Follow the project setup protocol exactly as defined in:

`docs/ai/setup/protocol.md`

Do not improvise the conversation structure — follow the protocol step by step.
