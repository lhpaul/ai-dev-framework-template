interface:
  display_name: "Workflow Item Orchestrator"
  short_description: "Advance one workflow item until it reaches a terminal condition."
  default_prompt: "Use $workflow-item-orchestrator to resume or advance one specific workflow item until it is waiting on a human, blocked, or escalated."

policy:
  allow_implicit_invocation: true
