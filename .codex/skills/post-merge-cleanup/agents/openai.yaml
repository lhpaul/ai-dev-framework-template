interface:
  display_name: "Post-merge cleanup"
  short_description: "Fetch, develop, pull, delete merged branch; update issue tracker."
  default_prompt: "Run post-merge cleanup (fetch, develop, pull, delete local merged branch), then update the related issue in the issue tracker if the branch name contains an issue ID."

policy:
  allow_implicit_invocation: false
