interface:
  display_name: "Workflow Orchestrator"
  short_description: "Advance workflow items until they reach a real terminal condition."
  default_prompt: "Use $workflow-orchestrator to inspect workflow state and keep advancing eligible items until they are waiting on a human, blocked, or escalated."

policy:
  allow_implicit_invocation: true
