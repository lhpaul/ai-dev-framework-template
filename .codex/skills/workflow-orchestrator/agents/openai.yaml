interface:
  display_name: "Workflow Orchestrator"
  short_description: "Batch and supervise workflow items until each reaches a terminal condition."
  default_prompt: "Use $workflow-orchestrator to inspect workflow state, build safe parallel batches, and supervise each eligible item until it is waiting on a human, blocked, or escalated."

policy:
  allow_implicit_invocation: true
