- **HTTP Chat Completions backend for local-ai-reviewer** (#1711): the Step 7 local reviewer can call an HTTP `/chat/completions` API (DeepSeek, Qwen, GLM, OpenAI, or similar) via `LOCAL_AI_REVIEWER_BACKEND=http` instead of the Codex CLI, while keeping Codex as the shared default. Deprecated alias: `openai_compat`.
