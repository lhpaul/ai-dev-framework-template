- **OpenAI-compatible backend for local-ai-reviewer** (#1711): the Step 7 local reviewer can call an OpenAI-compatible HTTP API (DeepSeek, Qwen, GLM, or similar) via `LOCAL_AI_REVIEWER_BACKEND=openai_compat` instead of the Codex CLI, while keeping Codex as the shared default.
