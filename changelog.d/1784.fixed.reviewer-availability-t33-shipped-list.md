- **`test-resolve-reviewer-availability.sh` T-33 / T-44 no longer assumes every driving runner has a reachable native reviewer against the shipped config** (#1784): the check iterated `driver in ('claude','cursor','codex')` against this repository's shipped `.ai-dev-workflow.yaml` and asserted `OUTCOME != 'blocked'` for all three, but commit `7fb67d72` deliberately narrowed the shipped `review.on_draft.runner` list to `[claude]`. For a driver not on that list, `resolve-reviewer-availability.sh` correctly falls through to a local-runtime probe for the configured `claude` entry rather than treating the driver as its own native reviewer; when that probe finds `claude` genuinely absent from `PATH` (as in this test's hermetic environment, and as GitHub Actions runners are today), the resolver legitimately reports `OUTCOME=blocked`, `BLOCK_CAUSE=zero-reachable`. T-33 / T-44 now derives the shipped runner list from the shipped config itself and asserts the correct, opposite-sign outcome for a driver outside that list (blocked / zero-reachable / `runtime-absent`) instead of assuming every driver resolves reachable. This is the same test/assertion failure reported from the CI-symptom side in #1792 ("`claude` CLI not on PATH in GitHub Actions runners") — the resolver was working as designed; the test's expectation was wrong. No change to `.ai-dev-workflow.yaml` or to `resolve-reviewer-availability.sh`'s behavior.
