- **Protocol 91 `pgrep` wait-loop guidance**: document the bracket trick
  (`pgrep -f "[p]r-review-loop.sh <PR>"`) so a wait loop cannot match its own
  shell command line, and correct the status-check contract so agents capture
  `pgrep`'s exit code inside the loop in `pgrep_rc` (`1` = no match / step
  ended; `2`/`3`/`127` = polling failure) instead of reading `$?` after
  `while pgrep ...; do ...; done` (which is the last body command, not
  `pgrep`). Uses `pgrep_rc` rather than `status` because zsh treats `status`
  as a read-only special parameter.
