- **Workflow test harnesses in consumer repositories** (#1631): Suites that encoded this template's own defaults now read the live `.ai-dev-workflow.yaml` or skip in downstream repositories, so a consumer that replaces the placeholder `deploy.yml` / `e2e-regression.yml`, drops `pr-policy.yml`, or configures `local-ai-reviewer` without `pr-agent` keeps the `workflow test harnesses` check green after syncing the template. The harness workflow also installs PyYAML before suites that parse workflow YAML, and its run summary no longer trips ShellCheck SC2016 under actionlint.
