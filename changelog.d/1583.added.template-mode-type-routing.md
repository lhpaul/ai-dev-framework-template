- **Framework-mode work-item classification** (#1583): refuse Type Workflow on backlog creation in template repositories, stop or hold misclassified Backlog routing, and treat open-framework-item discovery as all open board items with distinguishable lookup-unavailable reporting in framework mode. `add-backlog-item.sh` and its Linear `create` handoff refuse `--type Workflow` (exact, case-sensitive) before creation whenever `template.is_template: true`, with no bypass flag or override env var. `list_open_framework_items.sh` is the new entrypoint for release/retrospective "open framework items" reads: consumer repositories delegate to the unchanged `list_open_workflow_type_issues` (Workflow-only filtering), while framework mode returns every open non-terminal board item regardless of Type and reports one of nine closed-list `unavailable` causes when the lookup cannot be performed. `framework-mode-backlog-type-gate.sh` is the new Backlog routing gate: a framework-mode Backlog item typed Workflow with no development-folder artifacts and no implementation branch/PR evidence stops the single-item runner (`missing_tracker_context`) or holds it in a portfolio scan (`DISPATCH=held`), while a stale Backlog with artifacts, in-flight fast-track work, or any recognized non-Backlog status continues unchanged — nothing fails closed on an unreadable status, Type, or branch/PR evidence. Protocols `00`/`05`/`06`/`06b`/`90`/`91`, the tracker integration guide, `AGENTS.md` (and its `CLAUDE.md`/`GEMINI.md` symlinks), both orchestrator agent guidance files, and the affected smoke runbooks are updated so no surviving guidance points a framework-mode repository at Type Workflow.
