- **`test-step7a-surface-consistency.sh` D-9 no longer hardcodes the shipped draft-review runner list** (#1781): the check previously asserted `.ai-dev-workflow.yaml`'s `review.on_draft.runner` equals a literal `[claude, cursor, codex]`, so the deliberate reviewer-policy change to `[claude]` (#7fb67d72) turned develop red for every open PR. D-9 now parses the documented "shipped list" from `docs/workflow/development-workflow/README.md` and compares it against the actual configured list, so an intentional and documented reviewer-policy change stays green while a real drift between the documentation and the configuration still fails the check. README's stale `[claude, cursor, codex]` claim is corrected to match the shipped `[claude]` configuration.
