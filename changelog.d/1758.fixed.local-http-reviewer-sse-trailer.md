- Local HTTP AI reviewer now normalizes OpenAI-compatible proxy SSE responses that would otherwise escalate as `malformed_output`: trailing `data: [DONE]` on non-streaming JSON bodies, and full `chat.completion.chunk` SSE streams returned even when `stream=true` was not requested (seen with 9router on long combo-model reviews).
