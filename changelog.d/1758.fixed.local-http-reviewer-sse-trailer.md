### Fixed

- Local HTTP AI reviewer now strips trailing `data: [DONE]` SSE trailers that some OpenAI-compatible proxies (including 9router) append to non-streaming chat-completion bodies, so a valid verdict is no longer escalated as `malformed_output`.
