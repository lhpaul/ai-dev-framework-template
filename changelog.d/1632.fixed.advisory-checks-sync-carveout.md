- **Protect downstream advisory-check hooks from template sync** (#1632): treat
  `run-advisory-checks.sh` as project-specific so `/sync-template` no longer
  overwrites a project's implementation with the template no-op.
