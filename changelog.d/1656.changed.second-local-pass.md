- Before the ready-phase gate (`ensure_pr_ready_for_ready_phase`), the reviewer loop may dispatch the local reviewer once more when its most recent verdict is not clean on the current `loop_head_sha`, producing current-head evidence after a fix landed rather than refusing (#1656). Emits `LOCAL_SECOND_PASS` / `LOCAL_SECOND_PASS_REASON` telemetry and persists `local_second_pass_failed_head` in the ledger when a second pass fails on an unchanged head.
- The ready-phase local-pass guard re-reads the live PR head before every proceed path, including `not_required` and `no_local_reviewer`, and fails closed on a mismatch or an unreadable head.
