- **`local-ai-reviewer`: do not escalate `quota_exhausted` when stdout carries a review verdict** (#1762): the reviewer now skips the stderr setup probes when stdout is a review-result JSON object (`result`/`reviewed_head`/`findings`/`comments`/`issues`), preventing false-positive `quota_exhausted` escalations triggered by quota-related prose inside the reviewed document (e.g., a spec that mentions "usage limits") being echoed to stderr by the underlying CLI. Schemaless JSON keeps the probes, so genuine provider failures still escalate distinctly.
