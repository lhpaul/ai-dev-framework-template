- **`local-ai-reviewer`: do not escalate `quota_exhausted` when stdout contains valid review JSON** (#1762): the reviewer now clears the stderr probe window when the command produces valid JSON in stdout, preventing false-positive `quota_exhausted` escalations triggered by quota-related prose inside the reviewed document (e.g., a spec that mentions "usage limits" or "polling budgets") being echoed to stderr by the underlying CLI.
