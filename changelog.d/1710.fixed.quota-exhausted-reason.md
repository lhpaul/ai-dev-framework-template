- **`local-ai-reviewer`: classify provider quota exhaustion distinctly** (#1710): when the bundled reviewer CLI exits non-zero with empty JSON and a usage/quota refusal on stderr (Codex: `You've hit your usage limit` / `try again at …`), the companion now emits `REASON=quota_exhausted` and optional `QUOTA_RESET_AT` instead of `malformed_output`. `pr-review-loop.sh` forwards `QUOTA_RESET_AT` on escalate. Empty output without that signature stays `malformed_output`.
