- **Always-sync `workflow-tests.yml`** (#1630): include the selector-driven
  workflow test harness CI in `sync-manifest.yaml` so downstream template syncs
  receive the workflow that actually runs the suites.
