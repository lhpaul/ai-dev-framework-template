### Step 4

If the runner does not support native handoff, continue for each item one at a time.

Under Cursor `cursor-parent-orchestrated`, the current context has absorbed the portfolio layer: do not dispatch Work Item Runners; continue one at a time by following the item-layer protocol directly, from a stage role, only when a Cursor dispatch profile is declared.
