## Serialization note

ASSERT:router_grammar

Router grammar: arguments are split on commas, edge whitespace is trimmed, empty pieces are dropped, and duplicates are removed keeping the first occurrence.
