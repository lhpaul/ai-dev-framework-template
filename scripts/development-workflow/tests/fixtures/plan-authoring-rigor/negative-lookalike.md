# Negative lookalike fixture

Some intro text with no real Rule 1 heading.

```text
### Rule 1 — this heading-shaped line is inside a fenced code block
```

<!-- ### Rule 1 — this heading-shaped line is inside an HTML comment -->

No real `### Rule 1` heading appears anywhere in this document outside a
fence or a comment. The harness's heading counter must not count either
lookalike above.
