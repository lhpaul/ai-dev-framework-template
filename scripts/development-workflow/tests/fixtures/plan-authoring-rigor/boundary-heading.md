# Boundary heading fixture

### Rule 1 — Sampling an external output distribution

Some prose mentioning Rule 1 — foo inline, and Rule 10 as a different heading
below. Neither of those two prose mentions is a heading.

### Rule 10 — Not a real rule (edge case only)

This fixture exists to prove the harness's heading counter counts only the
real `### Rule 1` heading line above, and does not also count the substring
`Rule 1` that appears inside `### Rule 10`.
