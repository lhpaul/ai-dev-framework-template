interface:
  display_name: "Run item work (deprecated)"
  short_description: "Deprecated alias for $run-item — identical behavior; prefer $run-item."
  default_prompt: "Deprecated: use $run-item instead. $run-item-work advances one workflow item with the same bounded prelude and Protocol 91 behavior as $run-item."

policy:
  allow_implicit_invocation: false
