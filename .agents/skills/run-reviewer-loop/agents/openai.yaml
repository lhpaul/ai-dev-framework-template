interface:
  display_name: "Run reviewer loop"
  short_description: "Run automated reviewer and CI loops for a PR."
  default_prompt: "Use $run-reviewer-loop to drive the current branch's PR, or the PR number I provide, to readiness or escalation."

policy:
  allow_implicit_invocation: false
