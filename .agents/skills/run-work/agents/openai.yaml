interface:
  display_name: "Run work"
  short_description: "Inspect workflow state and advance eligible items."
  default_prompt: "Use $run-work to inspect workflow state, build safe batches, and advance eligible items until each reaches a terminal condition."

policy:
  allow_implicit_invocation: false
